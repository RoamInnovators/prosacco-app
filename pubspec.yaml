name: prosacco_mobile
description: ProSacco member mobile application (Flutter).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.3
  mobile_scanner: ^7.2.0
  webview_flutter: ^4.13.1
  firebase_core: ^4.6.0
  firebase_messaging: ^16.1.3
  pdf: ^3.11.1
  printing: ^5.13.1
  path_provider: ^2.1.4
  share_plus: ^12.0.2
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
